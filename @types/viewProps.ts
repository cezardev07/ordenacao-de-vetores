import { vetorProps } from "./vetorProps";
// importa o tipo para o method bubbleSort

export type viewProps = {
  BubbleSort?: vetorProps;
  SelectionSort?: vetorProps;
  InsertionSort?: vetorProps;
};