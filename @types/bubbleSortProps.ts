import { vetorProps } from "./vetorProps";
// importa o tipo para o method bubbleSort

export interface bubbleSortProps{
  bubbleSort: (vetor: vetorProps) => vetorProps;
}; // Declara um tipo para o method bubbleSort
